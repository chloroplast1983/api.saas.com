<?php
namespace Trade\Translator;

use System\Classes\Translator;

class CartTranslator extends Translator
{

    public function translate(array $expression)
    {

        // $cart = new \Trade\Model\Cart($expression['cartId']);
        // $cart->setNumber($expression['number']);
        // $cart->setCreateTime($expression['createTime']);
        // $cart->setProduct(new \Product\Model\Product($expression['productId']));
        // // $cart->setProductPrice(new \Product\Model\);
        // $cart->setGuest(new \Web\Model\Guest($expression['guestId']));
        // return $cart;
    }
}
